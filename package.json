{
  "name": "gala-burns",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ gala-burns",
    "create-local": "graph create --node http://localhost:8020/ gala-burns",
    "remove-local": "graph remove --node http://localhost:8020/ gala-burns",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 gala-burns",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.49.0",
    "@graphprotocol/graph-ts": "0.29.1",
    "@openzeppelin/subgraphs": "^0.1.8-5"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
